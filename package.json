{
  "name": "conduit-playwright-framework",
  "version": "1.0.0",
  "description": "Playwright E2E testing framework for Conduit application",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "test:chromium": "playwright test --project=chromium",
    "test:firefox": "playwright test --project=firefox",
    "test:webkit": "playwright test --project=webkit",
    "test:report": "playwright show-report",
    "test:allure": "node scripts/generate-allure-report.js generate allure-results --clean -o allure-report && node scripts/generate-allure-report.js open allure-report",
    "test:allure:generate": "node scripts/generate-allure-report.js generate allure-results --clean -o allure-report",
    "test:allure:serve": "node scripts/generate-allure-report.js serve allure-results",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write \"**/*.{ts,json,md}\"",
    "bootstrap:user": "ts-node --project tsconfig.json scripts/bootstrapUser.ts"
  },
  "keywords": [
    "playwright",
    "e2e",
    "testing",
    "typescript"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^2.15.1",
    "eslint": "^8.54.0",
    "prettier": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.2"
  },
  "dependencies": {
    "@faker-js/faker": "^8.3.1"
  }
}
